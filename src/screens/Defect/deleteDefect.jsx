import React from 'react'

const DeleteDefect = () => {
    return (
        <div>
            onClick={() => window.confirm( "Are you sure you want to delete this Defect? ") && this.deleteDefect(book.defectId)}
        </div>
    )
}

export default DeleteDefect
